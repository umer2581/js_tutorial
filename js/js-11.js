// undefined
 let umer;
 console.log(umer);
 var umer1;
 console.log(umer1);
//  const umer2;
//  console.log(umer2); this will be give error 

// null
 let khan=null;
 console.log(typeof khan); // this will give object and its a bug in js which will be not correct 
 console.log(khan);

 // BIGINT
 // in bigint we have limited space to store our variable and to store big values we use bigint keyword
 // with bigint we cannnot use any other data type this will gives us error

 let number=BigInt(121213123);
 console.log(number);

 let num2=1234n;
 console.log(num2);
 // we have two methods to use bigint first one is use bigint function with value and the second is use n with the end of number
 
